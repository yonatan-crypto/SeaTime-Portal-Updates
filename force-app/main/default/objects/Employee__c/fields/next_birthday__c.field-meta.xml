<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>next_birthday__c</fullName>
    <formula>IF(
  DATE(YEAR(TODAY()), MONTH( Date_Of_Birth__c ), DAY( Date_Of_Birth__c )) &gt;= TODAY(),
  DATE(YEAR(TODAY()), MONTH( Date_Of_Birth__c ), DAY( Date_Of_Birth__c )),
  DATE(YEAR(TODAY()) + 1, MONTH( Date_Of_Birth__c ), DAY( Date_Of_Birth__c ))
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>next birthday</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Date</type>
</CustomField>
